func Fuzz(compressed []byte) (ret int) { defer func() { v := recover() if v == `too high ratio` { panic(v) } if v!= nil { ret = -1 } }() }() decompressed := util.Decompress(compressed) if len(decompressed)/len(compressed) > 100 { panic(`too high ratio`) } return 0 }